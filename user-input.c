#include <stdio.h>
int main(){
    int a;//we need to define the datatype for the varibale beforewe can input it's value
    scanf("%d",&a);//scanf is used take input from user &a means that we are taking input for 'a' %d says that datatype fora is integer

    float b;
    scanf("%f",&b); //datatype is float and we are taking input for 'b'

}